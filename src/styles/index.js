import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    input: {
        margin: 10,
    },
    title: {
        textAlign: 'center',
        fontSize: 28,
    },
    subtitle: {
        textAlign: 'center',
        fontSize: 14,
    },
    logo: {
        margin: 10,
        alignItems: 'center',
    },
});
