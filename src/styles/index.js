import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    input: {
        margin: 10,
    },
    title: {
        textAlign: 'center',
        fontSize: 28,
        padding: 10
    },
    subtitle: {
        textAlign: 'center',
        fontSize: 14,
    },
    logo: {
        margin: 10,
        alignItems: 'center',
    },
    indicator: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        height: 80
    }
});
